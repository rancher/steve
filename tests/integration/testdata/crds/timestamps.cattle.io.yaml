---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: dates.timestamps.cattle.io
spec:
  group: timestamps.cattle.io
  names:
    kind: Date
    listKind: DateList
    plural: dates
    singular: date
  scope: Cluster
  versions:
  - name: v1
    additionalPrinterColumns:
    - jsonPath: .date
      name: Date
      type: date
      format: ""
    - jsonPath: .date
      name: Date
      type: date
      format: date-time
    - jsonPath: .date
      name: Date
      type: date
      format: date
    - jsonPath: .date
      name: Date
      type: string
    # Default date for CRDs (eg: when no columns defined)
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    # Default date for built-in (eg: ConfigMap)
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: string
    schema:
      openAPIV3Schema:
        type: object
        properties:
          date:
            type: string
    served: true
    storage: true
